# Release checklist

1. Run `npm run check`.
2. Update `CHANGELOG.md`.
3. Tag `v0.1.0` or the next semver version.
4. Push the tag and create a GitHub release.
5. Confirm GitHub Actions passes for `main` and the tag.
